
member:
  stage: build
  tags:
    - build
  # rules:
  #   - if: $CI_COMMIT_TAG
  # before_script:
  #   - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
  script:
    - node -v
    - npm -v
    - npm config get proxy
    - npm config rm proxy; npm install